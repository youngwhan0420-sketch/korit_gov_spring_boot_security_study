<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com_korit.security_study.mapper.OAuth2UserMapper">
    <resultMap id="OAuth2UserResultMap" type="com_korit.security_study.entity.OAuth2User">
        <id property="oauth2Id" column="oauth2_id"/>
        <result property="userId" column="user_id"/>
        <result property="provider" column="provider"/>
        <result property="providerUserId" column="provider_user_id"/>
        <result property="createDt" column="create_dt"/>
    </resultMap>

    <select id="getOAuth2UserByProviderAndProviderUserId" resultMap="OAuth2UserResultMap">
        select
            *
        from
            oauth2_user_tb
        where
            provider = #{provider} and
            provider_user_id = #{providerUserId};
    </select>

    <insert id="addOAuth2User">
        insert into
            oauth2_user_tb
        values
            (0, #{userId}, #{provider}, #{providerUserId}, now());
    </insert>
</mapper>